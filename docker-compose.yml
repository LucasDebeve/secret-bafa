services:
  web:
    build: .
    image: secret-bafa:latest
    container_name: secret-bafa
    restart: unless-stopped
    ports:
      - "${PORT:-8080}:80"
    networks:
      - caddy 

networks:
  caddy:
    external: true
